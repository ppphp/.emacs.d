;;; package --- Summary
;;; Commentary:
;;; Code:

;; global company init
(require 'org)

(setq org-agenda-files (list "~/.emacs.d/org/work.org"))
(setq org-log-done t)

(provide 'init-org)
;;; init-org.el ends here
